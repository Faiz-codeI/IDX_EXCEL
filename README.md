# IDX_EXCEL

A simple Streamlit app starter for IDX Excel.

## Run locally

```bash
pip install -r requirements.txt
streamlit run streamlit_app.py
```

## Deployment

Set the **Main file path** to `streamlit_app.py` when deploying with Streamlit Community Cloud.
